%h1 New Comment

= form_for @comment do |f|
  = f.label "Comment"
  %br
  = f.text_area :body, placeholder: 'Comment', cols: 100, rows: 5
  %br
  = f.hidden_field :impression_id, :value => @impression.id
  %br
  = f.submit 'Save and Parse'